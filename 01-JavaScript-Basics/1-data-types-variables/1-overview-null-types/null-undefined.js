// Extra credit: research "hoisting".

/* 
    null -> another primitive data type -> it means "without value" 
        "The value null represents the intentional absence of any object value" 
 */

    let whatIsNull = null;
    console.log(whatIsNull, "<---- are we getting this");
 /* 
    undefined -> another primitive data type -> 
        "undefined is a primitive value automatically assigned to variables 
        that have just been declared, or to formal arguments for which there are no actual arguments"
 */
    let whatIsUndefined;
    console.log(whatIsUndefined);
 /* 
    not defined -> this is an error that JavaScript environment will throw if you have a label and haven't initialized it
    with a value. 
 */
    notDefined2

