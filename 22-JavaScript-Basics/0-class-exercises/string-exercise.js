// Variable Exercises 
 
// For each exercise you'll want to log the result to the console to check your work

// 1. Concatenate "hello" with "goodbye"
 
// 2. Save the substring "Friend" from the string "Hello Friend"
 
let fS = "Hello Friend other";
const newString = fS.slice(fS.indexOf("F"), fS.indexOf("d")+1);

console.log(newString);
// 3. Use a template literal with the following variables to create a legible sentence.
const hello = "Hello"
const world = "World"
const my = "My"
const name = "name";
const is = "is"; 
let myName;